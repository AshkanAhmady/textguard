---
"@textguard/core": patch
---

Add a backward-compatible concise Debug signal projection and an opt-in timeline mode that omits rules with no matches while preserving the existing raw trace and default timeline semantics.
