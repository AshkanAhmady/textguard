# @textguard/cli

Initial command line interface for TextGuard.

## Usage

```bash
textguard scan "text to scan"
```

The CLI is an adapter over `@textguard/core` and does not contain detection rules.
